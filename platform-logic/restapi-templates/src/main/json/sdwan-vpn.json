{
    "vpns": [
        {
            "name": ${prop.name},
            "siteIds": [
                ${repeat:prop.siteIds_length:
                 ${prop.siteIds[${1}]},
                 }
            ]
        }
    ]
}